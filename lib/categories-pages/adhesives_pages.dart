import 'package:flutter/material.dart';

class AdhesivesPage extends StatelessWidget {
  const AdhesivesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('glue'),
      ),
      body: const Center(
        child: Text('glue'),
      ),
    );
  }
}
